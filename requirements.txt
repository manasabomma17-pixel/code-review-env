# Core
pydantic>=2.0.0
PyYAML>=6.0.0
python-dotenv>=1.0.0

# Web server (HF Spaces)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# OpenAI client (used for HF Inference API too)
openai>=1.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
